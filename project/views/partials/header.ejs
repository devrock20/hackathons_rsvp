<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel = "stylesheet" href= "/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <title>Charlotte Hackathons Meetup</title>
</head>
<body>
<main>
    <header>
        <nav>  
            <ul>
                <li class="nav-image"><a href="/"><img src = "/images/new_logo.png" alt="site-brand"></a></li>
                <li class="nav-center">
                    <h1>Charlotte Area Hackathons</h1>
                </li>
                <%if(!user) {%>
                    <li><a href='/users/new'>Sign Up</a></li>
                    <li><a href='/users/login'>Login</a></li>
                <%}else{%>
                    <li><a href='/users/profile'>Profile</a></li>
                    <li><a href='/users/logout'>Logout</a></li>
                    <li><a href="/hackathons/new">Start a new connection</a></li>
                <%}%>
                
            </ul>
        </nav>
        <div class ="bottomheader">
            <a href="/hackathons">Connections</a>
        
        <% if(userFirstName) {%>
            <strong>Welcome <%=userFirstName%></strong>
        <%}%>
        </div>
        <% if(errorMessages.length > 0) {%>
            <% errorMessages.forEach(message=>{%>
                <div class='error'>
                    <%=message%>
                </div>
                <%});%>
        <%}%>
        
        <% if(successMessages.length > 0) {%>
            <% successMessages.forEach(message=>{%>
                <div class='success'>
                    <%=message%>
                </div>
            <%});%>
                                    
         <%}%>
    
       
    </header>
    
    
    

</main>